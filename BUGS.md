# Bug Report

## Endpoints Tested
`GET /products/categories` — Get-all-Products-categories
https://dummyjson.com/docs/products#products-categories

## Summary

No valid bugs were identified during testing of the assigned endpoint. All requests returned expected status codes, response times, and response structures consistent with the documented behavior of the DummyJSON API (including its documented simulated-write behavior for `POST`/`PUT`/`DELETE` endpoints, where applicable).

The table below is retained per the required bug-report structure, with no rows populated, to confirm that the format was reviewed and intentionally left empty rather than omitted.

## Bug Report Table

| ID | Title | Endpoint | Severity | Steps to Reproduce | Expected Result | Actual Result | Status |
|----|-------|----------|----------|---------------------|------------------|----------------|--------|
| —  | —     | —        | —        | —                   | —                | —              | No bugs found |

## Notes

- Testing covered: status code validation, response time, response body structure, schema validation, and category-existence checks for the `groceries` category.
- No differences were found between expected and actual API behavior for this endpoint.
- This document should be updated if future test runs (e.g., expanded error-handling or auth/authorization scenarios) surface any defects.
